* mmap 虚拟内存-物理内存映射  
	*  分为共享映射和私有映射两种。
		*  共享映射:所有进程可见，需要自己实现锁。 私有映射: Copy On Write(COW)机制

* mmu  实现虚拟地址转换实际物理地址,页表记录映射信息。 
	* 读取数据时，如果未分配物理地址，产生缺页中断，切换到内核态读取数据，分配物理内存，内存不足时执行swap。

* dma  DMA是一种无需要CPU的参与就可以让外设与系统内存之间进行双向数据传输的硬件机制，使用DMA可以使系统CPU从实际的I/O数据传输过程中摆脱出来，从而大大提高系统的吞吐率

* mesi 多cpu缓存一致性
